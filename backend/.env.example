# .env.example

# --- Database Configuration ---
# PostgreSQL connection URL
# Example: postgresql://user:password@host:port/database?schema=public
DATABASE_URL="postgresql://YOUR_DB_USER:YOUR_DB_PASSWORD@YOUR_DB_HOST:YOUR_DB_PORT/YOUR_DB_NAME?schema=public"

# --- NestJS Application Configuration ---
# Port the NestJS application will listen on
PORT=3000

# Global prefix for API routes (e.g., /api)
GLOBAL_API_PREFIX=api

# --- Authentication Configuration ---
# Secret key for JWT signing. GENERATE A STRONG, UNIQUE KEY FOR PRODUCTION!
JWT_SECRET="super-secret-jwt-key-change-this-in-production"
# Expiration time for JWT tokens (e.g., '1d', '1h', '30m')
JWT_EXPIRATION_TIME="1d"
# Salt rounds for bcrypt password hashing
BCRYPT_SALT_ROUNDS=10

# --- Notifications (e.g., Email Service) ---
# Example for a transactional email service (e.g., SendGrid, Mailgun)
# EMAIL_SERVICE_API_KEY="your_email_service_api_key"
# EMAIL_SENDER_ADDRESS="no-reply@yourshop.com"

# --- Firebase Configuration (if used for notifications directly, though usually handled on frontend or dedicated service) ---
# If you are integrating Firebase for backend services (e.g., FCM for push notifications)
# FIREBASE_TYPE="service_account"
# FIREBASE_PROJECT_ID="your-project-id"
# FIREBASE_PRIVATE_KEY_ID="your-private-key-id"
# FIREBASE_PRIVATE_KEY="your-private-key" # BE CAREFUL WITH NEWLINES IN .ENV
# FIREBASE_CLIENT_EMAIL="your-client-email"
# FIREBASE_CLIENT_ID="your-client-id"
# FIREBASE_AUTH_URI="your-auth-uri"
# FIREBASE_TOKEN_URI="your-token-uri"
# FIREBASE_AUTH_PROVIDER_X509_CERT_URL="your-cert-url"
# FIREBASE_CLIENT_X509_CERT_URL="your-client-cert-url"
